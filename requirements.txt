google-cloud-pubsub
google-cloud-firestore
google-cloud-storage
Levenshtein
beautifulsoup4
requests
google-genai
pydantic-settings
jsonschema
pytest
pytest-asyncio
opentelemetry-api
opentelemetry-sdk
opentelemetry-exporter-otlp-proto-grpc
opentelemetry-resourcedetector-gcp

# evidence_agent.py (Sprint 4)
# Pinado (Sprint 8, Parte B) -- Dockerfile.evidence roda `playwright
# install --with-deps chromium` na mesma build, com a MESMA versao que o
# pip acabou de instalar; sem pin, uma rebuild futura poderia puxar uma
# versao mais nova do pacote Python cujo instalador de browser mudou de
# comportamento, sem isso ficar visivel no diff. Versao confirmada
# instalada e testada neste sprint.
playwright==1.62.0  # screenshot full-page headless, navegacao restrita ao dominio alvo
dnspython           # DNS A/AAAA/NS/MX/TXT + lookup de ASN via Team Cymru (DNS TXT, sem API key)
cryptography        # parse da cadeia de certificado TLS (emissor/validade/SANs)

# certstream==1.12 -- REMOVIDO. `plane1_ingestion/ct_listener.py` trocou o
# websocket do certstream.calidog.io (fora do ar, sem replay) por polling
# direto do log RFC 6962 (ver plane1_ingestion/ct_rfc6962.py e
# config.py::ct_log_base_url). `cryptography` (ja listado acima, usado por
# evidence_agent.py) e `requests` (ja listado acima) cobrem toda a leitura
# nova -- nenhuma dependencia adicionada so por causa desta troca.

# agent_gateway.py (Sprint 8, Parte A)
fastapi           # unico servico HTTP sincrono deste projeto alem do dashboard Next.js
uvicorn[standard] # servidor ASGI -- comando de deploy real em infra/ (Parte B)
httpx             # so para tests/test_agent_gateway.py (fastapi.testclient.TestClient)
google-auth       # google.oauth2.id_token -- verificacao de ID token (Agent Identity do
                  # gateway). Ja vinha transitivo via google-cloud-firestore/pubsub, mas
                  # um import direto merece dependencia explicita (regra do CLAUDE.md).
