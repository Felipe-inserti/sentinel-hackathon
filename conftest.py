"""Ancora o rootdir do pytest: garante que a raiz do repo esteja em
sys.path para testes em subdiretorios (ex: tests/), que caso contrario so
teriam seu proprio diretorio inserido pelo import mode padrao do pytest."""
